{
	"name": "streamthru",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "workers-assets-gen -mode=go && go build -o ./build/app.wasm .",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^4.43.0"
	}
}